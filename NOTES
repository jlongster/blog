
* Autosave
* Make runsvdir run on boot
* Back up database

# Deployment

./bin/deploy

## On the server

sudo docker pull jlongster/jlongster4
cd /service/jlongster
sudo sv restart .

# Tests

mocha --harmony -b -R tap .built/tests/api.js