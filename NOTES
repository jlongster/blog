
* Autosave
* Make runsvidr run on boot
* Back up database

# Deployment

NODE_ENV=production gulp backend
NODE_ENV=production gulp frontend
boot2docker
docker build -t jlongster/jlongster4 .
docker push jlongster/jlongster4

sudo docker pull jlongster/jlongster4
cd /service/jlongster
sudo sv restart .

# Tests

mocha --harmony -b -R tap .built/tests/api.js